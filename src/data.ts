import { Case, EvidenceFile, GeneratedReport, SystemActivity } from "./types";

export const initialCases: Case[] = [];

export const initialFiles: EvidenceFile[] = [];

export const initialReports: GeneratedReport[] = [];

export const initialActivities: SystemActivity[] = [];

